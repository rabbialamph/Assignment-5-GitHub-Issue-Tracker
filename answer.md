1️⃣ What is the difference between var, let, and const?
var = global scoped, reassignable redeclarable, hoisted undefined.

let = block-scoped, reassignable, not redeclaration, hoisted but referance error

const = block-scoped, not reassign, not redeclaration, hoisted but referance error, array object modify but not reassign



2️⃣ What is the spread operator (...)?
array object all element/value expand separately 


3️⃣ What is the difference between map(), filter(), and forEach()?
forEach() array all element function run , not return
filter() array all element condition true retains element , return new array (condition true)
map() array all element transfrom and create new array , return new array



4️⃣ What is an arrow function?
javascript easy write function syntax
const a = (a, b) => a + b;


5️⃣ What are template literals?
javascript easy write multiline string and ${} variable can embad.